{{#if tags}}
    <div class="post-tags container medium">
        {{#foreach tags}}
            <a class="post-tag tag-{{slug}}" href="{{url}}" title="{{name}}">{{name}}</a>
        {{/foreach}}
    </div>
{{/if}}